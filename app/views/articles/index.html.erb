<center>
<div id="new-form">
  <h4>Articles</h4>
    <% @articles.each do |article| %>
        <%#= article.id %>
        <div id= "article_show">
      <p> Title:<%= link_to article.title, article, remote: :true %></p>
        </div>
    <% end %><br>
  <%= link_to "New Article", new_article_path, remote: :true, class: 'btn btn-info btn-sm' %>
</div>
</center>